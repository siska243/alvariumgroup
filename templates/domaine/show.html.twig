{% extends 'base.account.html.twig' %}
{% block title %}Domaine{% endblock %}

{% block body %}
<div class="d-flex justify-content-between">
	<h1 class="mb-0">Creation d'une offre</h1>
    <div class="d-flex">
<a href="{{ path('app_domaine_index') }}" class="btn btn-secondary">retourn à la liste</a>
<a href="{{ path('app_domaine_edit', {'id': domaine.id}) }}" class="btn btn-primary">Modifier</a>
{{ include('domaine/_delete_form.html.twig') }}
</div>

</div>

    <h1>Domaine</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ domaine.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ domaine.title }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ domaine.isActive ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ domaine.createdAt ? domaine.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>
{% endblock %}
