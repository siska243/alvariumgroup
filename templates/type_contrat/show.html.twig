{% extends 'base.account.html.twig' %}


{% block title %}TypeContrat{% endblock %}

{% block body %}
    <h1>TypeContrat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_contrat.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ type_contrat.title }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ type_contrat.color }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ type_contrat.description }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ type_contrat.createdAt ? type_contrat.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_contrat_index') }}">back to list</a>

    <a href="{{ path('app_type_contrat_edit', {'id': type_contrat.id}) }}">edit</a>

    {{ include('type_contrat/_delete_form.html.twig') }}
{% endblock %}
